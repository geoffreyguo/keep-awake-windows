[project]
name = "keep-awake-windows"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyautogui>=0.9.54",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
